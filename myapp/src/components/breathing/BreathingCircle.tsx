import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const BreathingCircle = () => {
  return (
    <View style={styles.container}>
      <Text>BreathingCircle</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { padding: 16 }
});
