import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const ThoughtCard = () => {
  return (
    <View style={styles.container}>
      <Text>ThoughtCard</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { padding: 16 }
});
