import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const MoodSelector = () => {
  return (
    <View style={styles.container}>
      <Text>MoodSelector</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { padding: 16 }
});
