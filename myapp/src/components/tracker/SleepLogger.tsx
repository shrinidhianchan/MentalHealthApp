import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const SleepLogger = () => {
  return (
    <View style={styles.container}>
      <Text>SleepLogger</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { padding: 16 }
});
