import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const SymptomToggle = () => {
  return (
    <View style={styles.container}>
      <Text>SymptomToggle</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { padding: 16 }
});
